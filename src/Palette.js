import React, { Component } from 'react';

export default class Palette extends Component {
  render() {
    return (
      <div className="Palette">
        <div className="Palette-colors">
          {/* Bunch of color boxes and footer eventually*/}
        </div>
      </div>
    );
  }
}
